query GetMenu($handle: String!, $language: LanguageCode!)
@inContext(language: $language) {
  menu(handle: $handle) {
    id
    title
    items {
      id
      title
      url
      type
      items {
        id
        title
        url
        type
      }
    }
  }
}
